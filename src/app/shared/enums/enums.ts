export enum Status {
  Open = 'opened',
  Closed = 'closed',
  InProgress = 'InProgress',
}

export enum Issue {
  Major = 'Major',
  Minor = 'Minor',
  NoImpact = 'No Impact',
}

export enum Participants {
  Carrier = 'CDMP-C',
  Forwarder = 'CDMP-F',
  CargoIQ = 'Cargo-IQ',
}
